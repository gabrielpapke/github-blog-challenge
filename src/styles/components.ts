import styled from 'styled-components'

export const InfoTags = styled.div`
  margin-top: 0.5rem;
  display: inline-flex;
  align-items: center;
  gap: 1.5rem;
`
export const Tag = styled.span`
  display: flex;
  align-items: center;
  gap: 0.5rem;
`
